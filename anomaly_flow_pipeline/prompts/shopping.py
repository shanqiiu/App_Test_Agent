"""
shopping.py — 购物类 APP 主题提示词

包含购物场景下的示例、页面拓扑、价格判断规则等主题特定内容。
未来添加出行/社交等主题时，创建对应的 travel.py / social.py 并覆写相同变量名即可。
"""

# ═══════════════════════════════════════════════════════════
# Phase 0: 动作驱动重写 Prompt（购物场景）
# ═══════════════════════════════════════════════════════════

ACTION_REWRITE_PROMPT = """你是一个 App 操作步骤描述优化专家。你的任务是将"页面状态快照"改写为"用户动作 → 系统响应"格式的动作驱动描述。

## 原始输入
- 当前页面描述: {ui_summary}
- 用户操作意图: {thought}
- 操作类型: {action_type}

## 改写要求
1. 格式: "用户[做了什么操作]，系统[如何响应]，当前页面展示[关键UI信息]"
2. 语言自然口语化，模拟真实用户的视角
3. **输入框/搜索框中已有的具体文字内容属于核心状态数据，必须逐字保留，不得泛化为"原内容""原词"等抽象表述**
4. 保留所有重要的数据（价格、商品名、数量、搜索词等具体文本）
5. 每步一句或两句，不超过80字
6. 不要使用开发/测试术语（exception, error, null, undefined等）
7. 不要假设用户看不到的信息
8. **过滤无关历史数据**：如果 ui_summary 中包含与当前操作意图（thought）无关的历史累积数据（如首页购物车角标显示"54件商品"但当前流程只买1件），不要在改写中保留这些无关数据

## ❌ 错误示例（禁止）
原始: "京东首页，顶部有搜索框，当前内容为'海尔洗衣机'"
❌: "用户在首页搜索框输入'华为手机'，系统清空原内容..."
   → 严禁将"海尔洗衣机"泛化为"原内容"，必须原样保留
✅: "用户在首页搜索框点击清除'海尔洗衣机'后输入'华为手机'，系统更新搜索词..."

原始: "页面底部导航栏包含购物车按钮，显示有54件商品"（但当前流程只加入1件）
❌: "用户在底部点击购物车按钮（角标54），系统跳转购物车展示54件商品..."
   → 54件是历史累积数据，与本次操作意图无关，不应保留
✅: "用户在底部点击购物车按钮，系统跳转至购物车页面"

## ✅ 正确示例
原始: "京东首页，顶部有搜索框，当前内容为'海尔洗衣机'。搜索框右侧有搜索按钮。页面展示多种商品推荐..."
改写: "用户在首页搜索框点击清除'海尔洗衣机'后输入'华为手机'，系统更新搜索词并跳转到搜索结果页，展示华为手机相关商品推荐列表"

原始: "页面展示筛选选项，包含服务/折扣、价格区间、配送至、品牌及全部分类。价格区间包含最低价、最高价和价格选项..."
改写: "用户打开筛选面板设置价格上限为3000元，系统展示服务/折扣、品牌等筛选选项，底部有确定按钮显示'1400+件商品'"

## 输出
只输出改写后的文本，不要包含其他内容，不要加引号包裹。"""


# ═══════════════════════════════════════════════════════════
# Phase 0: 页面补齐 Prompt（购物场景）
# ═══════════════════════════════════════════════════════════

PAGE_COMPLETE_PROMPT = """你是一个 App 操作流程专家。分析以下操作序列，判断是否需要补充关键页面。

## 操作序列步骤
{steps_text}

## 预期购物流程页面拓扑
{expected_topology}

## 标准电商下单链路
Home → Search → SearchResult → ProductDetail → Cart → Checkout → Payment → OrderDetail

## 要求
1. 对照预期拓扑和标准链路，找出缺失的关键页面
2. **ProductDetail（商品详情页）是必补页面**：如果序列中用户在列表页直接加购而没有经过详情页（含商品大图、规格选择、价格展示），必须补充
3. **Payment（支付页/收银台）是必补页面**：如果序列停在"提交订单"而没有支付选择/确认环节，必须补充
4. **OrderDetail（订单完成页）建议补充**：支付成功后的确认页面
5. 补充步骤应插入到最合理的相邻位置
6. 补充的描述应包含页面展示的关键信息（价格、规格、支付方式等）

## 输出格式（只输出 JSON）
{{
  "missing_pages": [
    {{
      "page_name": "<缺失页面名称>",
      "insert_after_step": <int, 在此步骤后插入>,
      "action_description": "<动作驱动描述，格式: 用户[动作]，系统[响应]，页面展示[关键信息]>"
    }}
  ]
}}

如果无缺失，返回 {{"missing_pages": []}}"""


# ═══════════════════════════════════════════════════════════
# Phase 2: 步骤生成 Prompt（购物场景）
# ═══════════════════════════════════════════════════════════

STEPS_GENERATION_PROMPT = """你是一个 APP 操作流程数据生成专家。根据用户操作轨迹数据，生成符合建模规范的操作步骤。

## 核心原则
**状态 → 操作 → 状态 因果链**：每一步先声明页面当前的初始状态（即上一步的最终状态），再描述用户在该状态下做了什么操作，最后描述操作后的最终状态。最终状态自然成为下一步的初始状态。

## Action 描述规范
每条 action 包含三个层次，按以下格式输出：

```
页面初始状态：[操作前页面的空间/内容状态，即上一步的最终状态]
操作：用户[在当前页面上的具体操作]
最终状态：[操作后页面空间/内容变化的结果]
```

操作来源于轨迹数据中的 thought（用户意图），描述用户实际执行的交互动作。

## 因果链示例

Step 1:
  页面初始状态：京东首页已加载，搜索框内有历史搜索词"海尔洗衣机"
  操作：用户清除旧搜索词，输入"华为手机"，点击搜索按钮
  最终状态：页面跳转至搜索结果页，展示华为手机相关商品列表

Step 2:
  页面初始状态：搜索结果页展示华为手机相关商品列表（继承 Step 1 最终状态）
  操作：用户点击筛选按钮，在价格区间最高价输入3000，选中"百亿补贴"，点击确定
  最终状态：页面刷新，展示符合筛选条件的百亿补贴华为手机列表

Step 3:
  页面初始状态：商品列表页展示多款百亿补贴华为手机（继承 Step 2 最终状态）
  操作：用户点击华为畅享70X 256GB商品右侧的加号按钮，加入购物车
  最终状态：商品加入购物车成功，页面仍停留在商品列表页

## 规则
1. 每步的"页面初始状态" = 上一步的"最终状态"（首步的初始状态从 ui_summary 提取）
2. "操作"来自 thought（用户意图），描述用户做了什么
3. "最终状态"是操作的结果，自然成为下一步的初始状态
4. **每步操作不超过 3 个具体动作**：一个步骤中不要堆砌 5+ 个不同操作（如"筛选+输入+确定+再确定+等待+点击"）。必要时拆分为多个步骤
5. 最后一步的最终状态是流程终点，无后续操作
6. 保留原始轨迹中的异常状态信息（加载失败、错误提示等）
7. 初始状态必须如实反映 ui_summary，不能假设为"空"/"默认"
8. **页面类型准确**：ui_summary 描述的是"热卖榜""商品列表"等页面时，最终状态不要凭空写成"跳转至商品详情页"或"跳转至热卖榜页面"——以 ui_summary 实际描述的页面为准

## ⚠️ 生成检查清单（每条 action 输出前自查）
对每条轨迹数据，按顺序完成以下检查：
- [ ] 本条 ui_summary 中是否明确描述了当前页面已有的内容/状态？（如"搜索框当前内容为'海尔洗衣机'"、"已选中'百亿补贴'"、"价格已设为3000"）
- [ ] 我刚才写的"页面初始状态"是否包含了 ui_summary 中描述的内容？
- [ ] 我是否错误地将"空"/"默认"/"无内容"写入了初始状态，而 ui_summary 明明描述了具体状态？
- [ ] 如果 ui_summary 描述输入框有旧内容，我是否在"操作"中体现了"清除旧内容→输入新内容"的变化？
- [ ] 本步骤的操作数量是否超过 3 个？如果是，拆分为多个步骤

## ❌ 致命错误（绝对禁止）
```
// ui_summary 明确写了: "京东首页，顶部有搜索框，当前内容为'海尔洗衣机'"
// ❌ 禁止输出: 页面初始状态：APP首页已加载，搜索框为空
// ✅ 正确输出: 页面初始状态：京东首页已加载，搜索框内有历史搜索词'海尔洗衣机'

// ui_summary 描述的是商品列表页/热卖榜上的加购操作
// ❌ 禁止输出: "最终状态：跳转至热卖榜页面"（加购所在页面就是热卖榜，不是跳转目标）
// ✅ 正确输出: "最终状态：商品加入购物车成功，页面仍停留在商品列表页"
```

## 输出步骤的字段规范（来自建模 Schema）

{step_schema_text}

## 用户操作轨迹

{step_data_text}

## 输出格式
直接输出 JSON 数组：
[
  {{"order": 1, "action": "页面初始状态：...\\n操作：...\\n最终状态：..."}},
  ...
]"""


# ═══════════════════════════════════════════════════════════
# Phase 3: 价格一致性 Prompt（购物场景）
# ═══════════════════════════════════════════════════════════

PRICE_CONSISTENCY_PROMPT = """你是一个电商流程数据质量检查专家。判断以下操作步骤中的多个价格是否属于数据不一致。

## 判断规则
- 多款不同商品各有不同价格 → ✅ 正常
- 同一商品**同一价格类型**在不同步骤变化 → ❌ 是不一致
- 同一商品同时出现**不同价格类型**（如：原价 vs 补贴价 vs 国补后价 vs 券后价 vs 总价）→ ✅ 正常，这是不同价格语义
- 补贴金额/优惠金额/减免金额 → ✅ 正常，和售价是不同概念
- 用户输入的筛选价格上限/下限 → 忽略，不应计入
- **核心原则**：判断是否不一致的核心是看"同一价格类型"是否变化。原价¥1648和补贴后价¥1400.8是同一个商品的两个不同价格类型，**不是不一致**。

## 示例

示例1 - 正常（多商品不同价格）:
步骤:
  Step 7: "展示畅享70X（1648元）和畅享70X活力版（1299元）等多款手机"
  Step 8: "用户点击华为畅享70X（1648元）"
提取到的价格: 1648, 1299
判断: 正常。两个不同商品有不同的价格，搜索结果页的正常展示。

示例2 - 正常（原价与补贴价共存，同一页面）:
步骤:
  Step 20: "订单确认页展示华为畅享70X手机，数量1件，总价¥1648元，补贴后价¥1400.8元，已优惠¥247.2元"
提取到的价格: 1648, 1400.8, 247.2
判断: 正常。同一订单确认页同时展示原价¥1648、补贴后价¥1400.8、优惠金额¥247.2，属于不同价格类型（原价/补贴价/优惠额），不是不一致。

示例3 - 正常（原价与补贴价跨步骤）:
步骤:
  Step 9: "商品详情页展示价格1648元"
  Step 11: "购物车显示补贴后价1400.8元"
提取到的价格: 1648, 1400.8
判断: 正常。Step 9展示原价，Step 11展示补贴后价，价格类型不同。

示例4 - 不一致（同一价格类型变化）:
步骤:
  Step 8: "购物车总价显示¥2999"
  Step 10: "提交订单总价显示¥3499"
提取到的价格: 2999, 3499
判断: 不一致。购物车和结算的总价（同一价格类型）不同且无合理解释。

## 待判断的流程
步骤:
{steps_text}

提取到的价格: {price_list}

请仅输出以下格式之一（不要其他内容）：
正常: <简要理由>
  不一致: <简要理由>"""


# ═══════════════════════════════════════════════════════════
# 购物场景页面拓扑
# ═══════════════════════════════════════════════════════════

SHOPPING_PAGE_TOPOLOGY = [
    "home", "search", "searchResult", "productDetail",
    "cart", "checkout", "payment", "orderDetail"
]

SHOPPING_STANDARD_FLOW = (
    "Home → Search → SearchResult → ProductDetail → "
    "Cart → Checkout → Payment → OrderDetail"
)