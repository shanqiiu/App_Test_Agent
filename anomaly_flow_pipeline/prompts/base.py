"""
base.py — 主题无关的通用提示词

以下提示词不随业务主题（购物/出行/社交等）变化，可在任意主题中复用。
主题相关的提示词在各主题文件中定义（如 shopping.py）。
"""

# ═══════════════════════════════════════════════════════════
# Phase 0: UTG 预处理器
# ═══════════════════════════════════════════════════════════

DATA_ALIGN_PROMPT = """你是一个 App 测试数据一致性专家。检查以下操作序列中所有步骤的 UI 描述，找出数据不一致问题并修正。

## 用户原始查询
{query}

## 操作序列步骤
{steps_text}

## 检查要点
1. 搜索的商品名称在各步骤中是否一致
2. 同一商品的价格在各步骤中是否一致（注意区分原价/补贴价/券后价等不同价格类型）
3. 购物车数量等数据是否逻辑自洽
4. 商品规格（颜色、容量、型号）在不同步骤是否一致
5. 配送信息（快递方式、预计送达时间、收货地址）在订单流程中是否一致
6. 描述与实际语义是否匹配

## 输出格式（只输出 JSON）
{{
  "issues": [
    {{
      "step_index": <int>,
      "field": "商品名称|价格|数量|规格|配送信息|其他",
      "original": "<当前不一致的值>",
      "corrected": "<修正后的值>",
      "reason": "<修正原因>"
    }}
  ],
  "consistency_summary": "<整体一致性评估，一句话>"
}}

如果没有发现问题，返回 {{"issues": [], "consistency_summary": "数据一致"}}"""


SEMANTIC_DEDUP_PROMPT = """你是一个 UI 页面去重专家。判断两个 UI 描述是否描述的是**同一个页面、同一次浏览行为**（仅是表述方式不同）。

A: "{summary_a}"
B: "{summary_b}"

判断标准：
- 如果两者描述的是同一个页面场景（如都是搜索结果页、都是商品详情页），且处于同一次浏览的同一状态 → same
- 如果两者描述的是不同页面，或同一页面但不同状态（如搜索结果 vs 筛选面板展开） → different
- 如果两者页面不同但**用户操作意图完全相同**（如从首页点购物车 vs 从消息页点购物车，都是为了进入购物车） → same

仅回答: same 或 different"""


CAUSAL_REPAIR_PROMPT = """你是一个 APP 操作流程连贯性修复专家。下面两步骤之间存在逻辑断裂——前一步结束时页面处于异常状态，后一步却直接开始正常操作，缺少恢复/过渡描述。

## 前一步（异常状态）
{prev_summary}

## 后一步（待修复）
{next_summary}

## 修复要求
1. 在后一步的"操作"之前，增加一句自然的恢复过渡描述
2. 过渡描述应体现用户或系统如何从异常状态恢复到正常操作状态
3. 示例过渡：
   - "用户点击'重试'按钮后页面重新加载，网络恢复正常"
   - "系统自动刷新页面，数据加载成功"
   - "用户返回上一页面后重新进入，页面正常展示"
4. 保持原有的"操作："和"最终状态："不变
5. 语言自然口语化，不引入开发/测试术语

## 输出格式
直接输出修复后的完整 action 文本，不要包含解释或 markdown 包裹。"""


# ═══════════════════════════════════════════════════════════
# Phase 1: 异常注入器
# ═══════════════════════════════════════════════════════════

DECISION_PROMPT = """你是一个 App 异常测试场景生成专家。给定一个 App 操作序列中每步的 UI 状态描述，以及一个待注入的异常场景描述，你需要选择在序列的哪一步注入该异常最合理。

## 异常场景描述
{anomaly_scenario}

## 操作序列 UI 状态描述（按时间顺序）
{steps_text}

## 决策原则
1. 选择与异常场景描述**最自然契合**的步骤
2. 优先选择用户刚完成关键操作后、进入关键页面的时机
3. 避免选在首页、加载中状态、纯输入步骤
4. 避免选在序列的末尾步骤
5. 避免选在 thought 包含"澄清"、"确认"的步骤（通常是非操作性的中间对话）
6. **持续性异常需保留展示空间**：若异常为网络失败、加载失败、白屏等持续性异常，注入点后至少保留 3 个步骤来展示异常影响，不得选在末尾 3 步内

## 输出格式（仅返回 JSON）
{{
  "injection_step": <int>,
  "reason": "<string>"
}}"""


REWRITE_PROMPT = """你是一个 App 异常测试场景生成专家。你需要改写 App 操作序列中**某一步**的 UI 描述（ui_summary），使其反映指定的异常场景状态。

改写时必须保证**前后步骤的逻辑连贯性** — 异常不能凭空出现、也不能凭空消失。

## 异常场景描述
{anomaly_scenario}

## 被改写步骤的信息
- 步骤索引: Step {step_index}
- 操作意图: {thought}
- 操作类型: {action_type}
- 原始 UI 描述: {original_ui_summary}

## ← 前一步骤的上下文（供参考，不要改写）
{prev_summary}

## → 后一步骤的上下文（供参考，不要改写）
{next_summary}

## 改写要求
1. **保持上下文逻辑连贯**：
   - 从前一步操作自然过渡到异常状态
   - 异常不能"凭空出现" — 应在描述中体现因果链
   - 异常状态能被后一步感知（后一步会看到异常的影响）

2. **保持原有页面核心结构** — 不要在描述中改变页面的基本布局

3. **语言自然口语化** — 模拟真实用户看到的界面反馈，而非开发/测试视角：
   ✅ "页面顶部提示'网络连接失败，轻触屏幕重试'，内容区域显示空白占位图"
   ✅ "商品价格显示为'¥0.00'，明显偏离正常价格范围"
   ❌ "系统抛出NetworkErrorException异常"  
   ❌ "数据库查询超时返回空结果集"
   ❌ "API接口返回500状态码"
   ❌ "JSON解析异常导致渲染失败"

4. **不改变操作意图（thought）** — 用户在做什么操作不变

5. **具体而非抽象** — 说明异常的具体表现（什么元素、什么位置、什么变化）

6. **持续性异常要体现残余影响** — 如果异常是网络失败、加载失败、白屏等持续性异常：
   - 描述中应包含"用户看到异常后的反应"，如"用户点击重试""等待恢复"
   - 后续步骤（通过邻步微调）应保持异常影响的涟漪效应，直到有明确恢复操作

## 输出格式
只输出改写后的 ui_summary 文本，不要包含其他内容。"""


NEIGHBOR_ADJUST_PROMPT = """你是一个 App 异常测试场景生成专家。操作序列的 {pos} 步（Step {step_index}）刚被注入了异常，现在需要微调 {pos} 步的描述来配合异常场景，使整体流程连贯自然。

## 被注入的异常场景
{anomaly_scenario}

## 异常步的描述（已改写）
{current_step_description}

## 需要微调的步骤信息
- 步骤索引: Step {step_index}
- 原始 UI 描述: {original_ui_summary}
- 操作意图: {thought}
- 操作类型: {action_type}

## 微调要求
1. 只做微调，不做大幅度改写
2. {pos} 步应自然衔接异常步（前一步导致异常 / 后一步体现异常影响）
   - 如果是前步：描述应自然引向即将发生的异常（如"点击确定后系统开始加载数据"）
   - 如果是后步：描述应体现异常的影响（如"页面仍显示加载失败提示，用户尝试点击重试"）
3. 保持语言自然口语化
4. 不要引入开发/测试术语

## 输出格式
只输出微调后的文本，不要包含其他内容。"""


VALIDATION_PROMPT = """你是一个 App 测试数据质量专家。检查以下改写后的异常步骤描述是否存在问题。

## 原始 UI 描述
{original}

## 改写后 UI 描述
{rewritten}

## 异常场景
{scenario}

## 检查项
1. 改写后是否为空或过于简短（<20字）？ [是/否]
2. 是否包含开发/测试术语（exception, error, null, API, 状态码等）？ [是/否]
3. 改写后是否保留了原始页面的核心结构信息？ [是/否]
4. 异常描述是否自然融入了页面描述（而不是生硬拼接）？ [是/否]
5. 是否存在明显的数据矛盾或逻辑不一致？ [是/否]

## 输出格式
{{
  "is_valid": true/false,
  "issues": ["<问题1>", "<问题2>"],
  "suggestion": "<改进建议>"
}}"""


# ═══════════════════════════════════════════════════════════
# Phase 2: Flow 转换器（主题无关部分）
# ═══════════════════════════════════════════════════════════

STEPS_COMPRESS_PROMPT = """你是一个 APP 操作流程编辑专家。合并相邻的**同页面**操作步骤，使流程更简洁。

## Action 描述规范
每条 action 按以下格式：

```
页面初始状态：[操作前页面状态]
操作：用户[在当前页面上的具体操作]
最终状态：[操作后页面状态]
```

### 合并后格式
同页面的多个操作合并为一步，操作用"依次"串联：

```
页面初始状态：[合并前第一步的初始状态]
操作：用户依次[操作1]、[操作2]…
最终状态：[合并后最后一步的最终状态]
```

## 合并规则
1. 识别相邻步骤是否发生在**同一个页面**上（根据 action 语义判断）
2. 如果是，合并为 1 个步骤
3. 如果相邻步骤发生在**不同页面**，保持独立，不合并
4. 合并后保留异常状态信息
5. **连续返回合并**：3+次连续"返回"操作合并为 "用户连续点击返回 N 次，从[起始页]退回至[终点页]"

## 示例

### 示例1：同页面操作合并
输入:
  Step 1: "页面初始状态：京东首页已加载，搜索框内有历史搜索词'海尔洗衣机'
    操作：用户清除旧词，输入'华为手机'
    最终状态：搜索框内容更新为'华为手机'，搜索按钮可点击"
  Step 2: "页面初始状态：搜索框内容为'华为手机'，搜索按钮可点击
    操作：用户点击搜索按钮
    最终状态：页面跳转至搜索结果页"
输出:
  [{{"order": 1, "action": "页面初始状态：京东首页已加载，搜索框内有历史搜索词'海尔洗衣机'\\n操作：用户依次清除旧词输入'华为手机'、点击搜索按钮\\n最终状态：页面跳转至搜索结果页"}}]

### 示例2：连续返回合并
输入:
  Step 10: "页面初始状态：当前为商品详情页
    操作：用户点击顶部返回按钮
    最终状态：系统返回上一层页面（搜索结果页）"
  Step 11: "页面初始状态：当前为搜索结果页
    操作：用户点击顶部返回按钮
    最终状态：系统返回上一层页面（首页）"
  Step 12: "页面初始状态：当前为首页
    操作：用户点击顶部返回按钮
    最终状态：系统返回上一层页面"
输出:
  [{{"order": 10, "action": "页面初始状态：当前为商品详情页\\n操作：用户连续点击返回按钮 3 次\\n最终状态：系统逐步退回至首页"}}]

## 待处理的步骤
{steps_text}

## 原始轨迹数据（参考页面布局用）
{utg_context}

## 输出
直接输出合并后的 JSON 数组（order 重新编号从 1 开始），不要 markdown 包裹：
[
  {{"order": 1, "action": "..."}},
  ...
]"""


STEPS_DEDUP_PROMPT = """你是一个 APP 操作流程精简专家。检测并合并以下流程中的冗余步骤。

## 可合并的冗余模式
1. **重复操作**：同一操作在同一步骤上下文中出现多次（如两次点击购物车图标、两次提交订单）
   → 保留第一次，删除后续重复
2. **无推进的中间步骤**：步骤的最终状态与初始状态完全相同，用户没有产生任何变化
   → 删除该步骤
3. **过度碎片化**：一个页面上的连续多个小操作可以合并为 1 步
   → 合并为"用户依次[操作1]、[操作2]..."
4. **页面不同但意图相同**：用户从不同入口到达同一目标页面的重复操作（如从首页点购物车 vs 从消息页点购物车）
   → 保留第一次有效到达，删除后续冗余

## 待精简的步骤
{steps_text}

## 输出格式
直接输出精简后的 JSON 数组（order 重新编号从 1 开始）：
[
  {{"order": 1, "action": "页面初始状态：...\\n操作：...\\n最终状态：..."}},
  ...
]

只删除/合并真正冗余的步骤，保留所有有实质推进的步骤。"""


ENTITY_EXTRACTION_PROMPT = """你是一个 APP 数据抽取专家。从操作步骤中提取关键业务实体信息，填充到建模模板的数据主题中。

## 实体的字段规范（来自建模 Schema）

{entity_schema_text}

## 操作步骤

{steps_text}

## 要求
1. 分析所有步骤中提到的具体业务实体（商品、服务、内容、商品等）
2. 从步骤描述中提取实体的具体属性值，严格遵循字段规范中的类型（string / number / price / enum 等）
3. 步骤中未提及的字段设为 null
4. 如果步骤中没有可提取的业务实体，返回空数组 []
5. 不捏造步骤中不存在的数据
6. 严格遵循输出 JSON 结构，不要增减字段

## 输出格式
直接输出 JSON 数组，不要 markdown 包裹：
[
  {{
    "instanceId": "entity_from_steps",
    "imageUrl": null,
    "values": {{ ... }}
  }}
]"""


# ═══════════════════════════════════════════════════════════
# Phase 3: 质量验证器（主题无关部分）
# ═══════════════════════════════════════════════════════════

HOLISTIC_VALIDATION_PROMPT = """你是一个 APP 操作流程质量诊断专家。对以下 mainFlow 的所有步骤进行五维度整体审视，每个维度给出等级（正常/轻微/严重/致命）和具体问题。

## 五维度定义

1. **衔接性**：相邻步骤是否构成"用户操作→系统响应"的因果链？是否有跳跃（状态突变无过渡）、断裂（异常后凭空恢复）、循环（反复在同一页面来回）？
2. **逻辑清晰性**：每步描述是否无二义、无内部矛盾？flow.name/description 与 steps 实际内容是否一致（如 description 说"iPhone"但 steps 全是"华为"）？
3. **内容重复性**：是否有相邻步骤描述同一页面同一状态？是否有连续相同操作（如连续点击返回3次以上）？
4. **顺序合理性**：流程是否符合业务逻辑？是否缺失关键页面、是否包含无关页面？
5. **数据一致性**：同一商品名称/价格/数量跨步骤是否一致？无 54件→1件 这种数量爆炸？不同价格类型（原价vs补贴价）是否清楚标注？

## 流程信息
- Flow 名称: {flow_name}
- Flow 描述: {flow_desc}
- 前置条件: {precondition}

## 所有步骤
{steps_text}

## 输出格式（仅输出 JSON，不要 markdown 包裹）
{{
  "overall_grade": "正常|轻微|严重|致命",
  "coherence": {{
    "grade": "正常|轻微|严重|致命",
    "issues": ["<问题1>", "<问题2>"]
  }},
  "clarity": {{
    "grade": "正常|轻微|严重|致命",
    "issues": ["<问题1>"]
  }},
  "redundancy": {{
    "grade": "正常|轻微|严重|致命",
    "issues": ["<问题1>"]
  }},
  "sequence": {{
    "grade": "正常|轻微|严重|致命",
    "issues": ["<问题1>"],
    "missing_pages": ["<缺失的关键页面>"],
    "irrelevant_steps": ["<无关步骤序号>"]
  }},
  "consistency": {{
    "grade": "正常|轻微|严重|致命",
    "issues": ["<问题1>"]
  }},
  "suggestions": ["<改进建议1>", "<改进建议2>"]
}}

如果没有问题，对应 issues 返回空数组 []，grade 返回 "正常"。"""